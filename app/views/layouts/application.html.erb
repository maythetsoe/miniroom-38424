<!DOCTYPE html>
<html>
  <head>
    <title>MiniRoom38424</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap" rel="stylesheet">
  </head>

 <body>
    <header class="header">
      <div class="inner">
        <div class="nav">
          <div class="nav__left">
            <%= link_to image_tag("minilogo.png", class: :logo), root_path %>
          </div>
          <% if user_signed_in? %>
          <%# ログインしているときは以下を表示するようにしましょう %>
           <div class="nav__right">
             <%= link_to "ログアウト", destroy_user_session_path, method: :delete, class: :nav__logout %>
             <%= link_to "Mini Room", new_miniroom_path, class: :nav__btn %>
             <%= link_to "Donation Room", "#", class: :nav__btn %><br><br> 
             <%= link_to "#{current_user.nickname}さん , Welcome To Minimal Life", "/users/#{current_user.id}", class: :nav__btn %>

            </div>
            <% else %>
          <%# // ログインしているときは上記を表示するようにしましょう %>
          <%# ログインしていないときは以下を表示するようにしましょう %>
            <div class="nav__right">
            <%# <a class="dropdown">
            <img alt="account" class="hover-off header-icon" srcset="account_logo.jpg"> %>
            <%# <span class = "header-letter">ログイン</span> %>
              <%= link_to "ログイン", new_user_session_path, class: :nav__btn %>
              <%= link_to "新規登録", new_user_registration_path, class: :nav__btn %>
            </div>
            <% end %>
          <%# // ログインしていないときは上記を表示するようにしましょう %>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer class="footer">
      <p class="copyright">Copyright © MINIROOM All rights reserved.</p>
    </footer>
  </body>
</html>
